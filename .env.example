# Example env file (DO NOT COMMIT - add to .gitignore)

# Frontend (Vite) - use VITE_ prefix so variables are available to client-side code
# Provide meaningful placeholder values (replace these with real secrets in your deployment UI)
VITE_GEMINI_API_KEY=sk-REPLACE_ME
VITE_ALPHAGENOME_API_KEY=ak-REPLACE_ME
VITE_ALPHAGENOME_API_URL=https://api.alphagenome.example
VITE_BACKEND_URL=http://localhost:5000
VITE_ALPHAFOLD3_API_URL=https://api.esmatlas.com/foldSequence/v1/pdb/  # optional: proxy for AlphaFold

# Backend (backend/.env)
# Use generic placeholders for credentials. Do not commit secrets.
DATABASE_URL=postgresql://db_user:db_pass@localhost:5432/mutationmechanic
FRONTEND_URL=http://localhost:3000
PORT=5000
JWT_SECRET=generate-a-strong-random-secret-keep-private

